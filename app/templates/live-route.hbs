<LeafletMap 
  @lat={{this.lat}} 
  @lng={{this.lng}} 
  @zoom={{this.zoom}} 
  @dragging={{false}}
  @zoomControl={{false}}
as |layers|>
  <GoogleMutantLayer @type="roadmap" />
  <layers.marker @location={{this.pinLocation}} />
</LeafletMap>

<NotificationPane 
  @delivery={{this.delivery}} 
/>